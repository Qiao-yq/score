# TASK 项目考核打分系统 PRD

版本：V1.0（规则冻结版）  
日期：2026-08-26  
状态：业务规则冻结；Agent 技术可行性待 M0.5 POC 验证  

> 规则基线：本 PRD 采用“单一 Agent 主评分 + 教师复核改分 + NO Push 算法互评”的唯一成绩口径。不存在多评委平均；提交速度为系统纯自动分；任何人工改分都不改变 Agent 原始版本，须按合成公式并经过相应审批。

## 1. 产品定义

### 1.1 产品目标

为比赛/课程项目提供团队资料提交、Agent 主导评分、队长团队互评、评语反馈和结果大屏能力。系统同时作为赛博朋克视觉、响应式布局、实时同步和数据可视化的展示项目。

### 1.3 Agent 主评分模式（本版核心规则）

- 系统只接入一个统一评分 Agent，不拆分多个独立 Agent 产品角色。
- Agent 统一读取团队报告、原型、视频、提交记录和性能测试结果，按照本 PRD 前六维规则生成评分；有效互评结果由 NO Push 算法独立计算后计入总分。
- Agent 负责生成前六项客观/主观维度分、总分建议、亮点、改进建议、证据引用和置信度；团队互评仍由确定性 NO Push 算法计算。
- Agent 产出的分数在系统中视为“正式候选成绩”；管理员发布前必须完成一次审核确认。
- 教师不再逐项主导打分，仅可对 Agent 结果进行复核、提出异议或修改。任何人工修改必须填写原因，并保留 Agent 原始版本。
- 默认教师复核分等于 Agent 分；教师修改后按合成公式重新计算，管理员确认后发布。
- Agent 调用失败、证据不足或置信度低于 0.70 时，成绩状态为 `needs_review`，不得自动发布。

`needs_review` 兜底：教师必须在截止后 3 个工作日内选择“认可 Agent”“修改分数”或“资料不足”。Agent 调用失败时教师可按同一 Rubric 人工补录前六维；资料不足的维度最高 60 分并填写原因。教师改分超过 20% 或 Agent 与教师差异超过 20 分时，由管理员二次确认；逾期未处理由管理员指定补评分。兜底来源标记为 `teacher_override` 或 `manual_fallback`，并保留全量版本。

### 1.2 V1 范围

- 团队、成员、项目资料管理
- Agent 按前六个维度评分并生成结构化评语，教师复核并可改分；第七项由 NO Push 互评算法计算
- 队长单次随机 NO Push 团队互评
- PC、移动端和 `/dashboard` 大屏实时同步
- 评分结果、排名和统计图表
- 权限、脱敏、操作日志和评分版本审计

语音转文字、Three.js 奖杯、音效和粒子爆炸属于 P2 增强项，不得阻塞核心评分流程。

## 2. 角色与权限

| 角色 | 核心权限 |
|---|---|
| 管理员 | 创建比赛、配置规则、导入团队、分配教师、锁定/解锁评分、处理异常、发布大屏、查看全部审计记录 |
| 教师 | 查看 Agent 评分证据和评语，进行复核、提出修改建议或提交异议；不能直接创建无 Agent 依据的评分 |
| 队长 | 维护团队资料，查看本队公开结果和“仅队长可见”评语，发起成员邀请 |
| 成员 | 查看团队资料和全员可见结果；不能执行队长互评或查看评分来源 |
| 观众 | 仅访问已发布的大屏公开数据 |

所有写操作必须经过服务端鉴权。评语和评分的可见性由服务端过滤，前端隐藏不构成权限控制。

## 3. 比赛与状态流转

管理员创建比赛并设置：名称、时区、提交截止时间、评分维度、互评开关和大屏发布开关。

团队状态：`draft`（草稿）→ `submitted`（已提交）→ `locked`（已锁定）→ `published`（已公示）。  
评分状态：`not_started` → `processing` → `agent_scored` → `needs_review`/`approved` → `published`。  
互评状态：`not_started` → `open` → `submitted` → `closed`。

- 截止时间后禁止团队修改资料；管理员可临时解锁并必须填写原因。
- Agent 评分和每次人工复核均生成不可变版本；最终版本由管理员锁定。
- 只有 `finalized` 的团队进入正式排名；大屏默认不展示未发布的分数。

## 4. 团队资料规则

必填：团队名称、队长、至少 2 名成员、项目名称、项目简介、报告链接。  
原型和演示视频为选填，但若配置为必交则在提交校验中强制要求。

- 团队名称在同一比赛内唯一，长度 2–50 字符。
- 成员在同一比赛内只能属于一个团队。
- 队长必须是本团队成员；一个团队只能有一名队长。
- 提交后生成不可变的版本快照，附件使用白名单类型和大小限制（单文件默认 200 MB）。
- 外链必须记录 URL、类型、提交版本和访问状态；不得在大屏展示原始附件地址。

## 5. Agent 主评分规则

### 5.1 评分维度与权重

Agent 和教师对前六个维度使用 0–100 的整数原始分，最终按权重换算，总分 100 分：

| 维度 | 权重 | V1 评分口径 |
|---|---:|---|
| 提交速度 | 10% | 系统依据提交时间相对截止时间自动计算 0–100 分；教师和 Agent 均不可修改 |
| 报告质量 | 30% | 完整性 10、准确性 10、可执行性 10；三项均为 0–10 分后换算 |
| 交互视觉 | 20% | 原型 25%、布局 25%、色彩 25%、精致度 25%；每项 0–100 后取平均 |
| 功能体验 | 15% | 完整度、流畅度、按钮辨识度各占 1/3；多端不一致时该项最高 60 分 |
| 技术性能 | 10% | 运行效率 50%、加载速度 50%；以测试记录为依据 |
| 演示效果 | 10% | 表现力 50%、答辩质量 50% |
| 团队互评 | 5% | 由队长单次随机评分产生，评分来源对团队不可见 |

计算公式：

```text
维度合成分 = Agent 分 × 70% + 教师分 × 30%
最终分 = Σ(前六维度合成分 × 维度权重) + NO Push 互评得分（满分 5）
发布分 = 管理员批准的最新合成版本
```

- 评分范围不得超出维度上限；所有校验在服务端执行。
- 提交速度自动分规则（原始分）：提前或准时 100 分；逾期 1 小时内 80 分；1–24 小时 50 分；24–72 小时 20 分；超过 72 小时 0 分。
- Agent 必须为前六维度生成分数、证据和评语；NO Push 互评维度只保存算法结果，不生成主观评分；亮点、改进建议各不少于 10 个字符。
- 每个维度至少需要 1 条可定位证据；无法定位证据时该维度最高 60 分并标记低置信度。
- 不再进行多评委平均；教师未修改时教师分默认等于 Agent 分，Agent 评分仍为主导。
- 同分时依次按报告质量、提交时间、团队名称排序。

### 5.2 评语

每个维度包含“亮点”和“改进建议”两个独立字段，可插入预置标签。V1 标签固定为：`#架构优雅`、`#交互丝滑`、`#文档缺失`、`#创意惊艳`。

可见性只能选择一种：

- 仅队长可见：队长可见，其他成员和大屏不可见。
- 全员可见：团队所有成员可见，大屏不可见。
- 公示到大屏：团队成员可见，并可在大屏脱敏展示。

Agent 评语和人工修改都会生成版本记录；大屏只读取最新已发布版本。Web Speech API 为可选能力，识别文本必须经教师确认后才能保存。

### 5.3 Agent 评分输出与复核

Agent 每次评分必须保存：`scoreVersion`、`rubricVersion`、输入资料版本、各维度分数、证据定位、亮点、改进建议、置信度、风险标记、模型版本、提示模板版本和生成时间。重新评分生成新版本，不覆盖历史结果。

Agent 生产安全与成本规则：学生项目预览和浏览器自动化必须运行在一次性隔离沙箱中，禁止挂载宿主机文件系统；限制 CPU、内存、磁盘、进程数和最长执行时间；网络默认拒绝，仅开放经审核的白名单出口，禁止访问内网和云元数据地址。附件在解析前必须通过 MIME/扩展名校验、病毒扫描和大小限制。每次 Agent 调用记录模型、Token、ASR/浏览器调用、耗时、重试次数和费用；比赛开始前必须配置单项目预算、最大并发和评审窗口吞吐目标，超预算或超时自动进入 `needs_review`。

- 教师可对任一维度标记“认可”“建议修改”或“资料不足”。
- 修改分数必须填写不少于 10 个字符的原因，并引用证据或测试记录。
- 教师可以修改任一前六维度分数，修改幅度不得超出 0–100 分范围，必须填写不少于 10 个字符的原因。
- 教师修改幅度超过该维度满分的 20% 时，必须由管理员二次确认；未确认前沿用 Agent 分。
- Agent 结果与教师分数差异超过 20 分，或任一维度置信度低于 0.70 时，自动进入 `needs_review`，不得直接发布。

### 5.4 证据定位规范

Agent 只能引用已抽取且带唯一编号的证据，不得凭空生成依据：

| 资料 | 定位要求 |
|---|---|
| PDF/Word/Markdown | 文件名、页码或章节、段落文本 |
| 图片/截图 | 文件名、区域坐标、OCR 文本 |
| 在线原型 | URL、路由、操作步骤、截图、DOM/可访问性节点 |
| 本地 Web 项目 | 浏览器自动化路径、操作步骤、截图、控制台日志 |
| 演示视频 | 文件名、起止时间戳、ASR 转录、关键帧 |
| 性能数据 | 测试工具、环境、指标名、原始报告定位 |

交互视觉和功能体验优先使用浏览器自动化与截图证据；不可访问时必须标记风险并进入人工复核。

### 5.5 置信度标定与跨比赛一致性

`0.70` 置信度和 `20` 分差异为 POC 初始阈值，不是永久常数。M0.5 使用至少 10 个历史/脱敏项目，由至少 2 名教师独立评分建立基线，计算各维度 MAE、教师间分歧、证据完整率、重复评分最大偏差和 Agent 置信度校准曲线。以基线误差的 P95 设定差异复核阈值，以覆盖 95% 高质量样本的最低置信度设定置信度阈值；同一输入连续 3 次评分的各维最大偏差须 ≤5 分。阈值经产品负责人确认后写入 `rubricVersion`。

所有比赛统一使用 0–100 原始分、同一 Rubric 版本、同一归一化方法；不同规则或模型版本的成绩不得直接横向比较。

## 6. NO Push 互评规则

### 6.1 队长单次随机互评

- 互评单位为团队，不评价团队内部成员。
- 每个团队仅由队长提交 1 个综合互评分，范围为 0–100 的整数。
- 比赛至少需要 2 个已提交团队；仅 1 个团队时关闭该项并由管理员按配置补录。
- 系统随机生成一组一对一映射：每个团队恰好评价 1 个其他团队，同时恰好被 1 个其他团队评价。
- 映射禁止自评、评价关系不重复，任意奇数或偶数团队数均可运行；不要求双方互相评价。
- 队长只看到被分配团队的匿名项目资料，不看到对方队长或评分来源身份。
- 评分截止后不可修改；管理员解锁必须填写原因并生成新版本。

### 6.2 双盲与异常检测

- 被评分团队不知道评分人身份，评分人不知道被评分团队的队长身份；管理员可查看映射用于审计。
- 评分人、被评分团队和分数映射不得出现在大屏或普通用户接口中。

提交时执行以下规则，命中任一项即标记 `suspicious`，该份互评暂不计分并通知管理员：

1. 评分不在 0–100 范围内或不是整数。
2. 映射出现自评、重复目标或团队未被覆盖。
3. 评分提交账号、设备或网络特征与目标团队存在明显关联。
4. 评分内容命中批量模板或与其他评分完全重复。
5. 管理员标记存在利益冲突或其他人工风险。

有效互评得分 = `该团队收到的有效队长评分 / 100 × 5`，结果限制在 0–5 分并四舍五入到 1 位。异常记录不计入发布结果；管理员可复核、作废或要求重新随机分配，并必须留下原因。

## 7. 实时同步与离线规则

### 7.1 同步范围

评分草稿、评分提交、评语版本、排名、进度和大屏公开数据通过 WebSocket 推送。服务端数据库是唯一事实源，Redis 用于连接状态、发布订阅和短期缓存。

### 7.2 消息规则

每条消息包含：`messageId`、`event`、`serverTime`、`competitionId`、`entityId`、`entityVersion`、`actorId` 和 `payload`。

- 客户端发送消息必须带 `clientMessageId`，服务端按该 ID 幂等处理。
- 服务端确认成功后客户端删除本地队列；失败消息保留并指数退避重试，最多 10 次。
- 冲突按服务端 `serverTime` 的最后写入优先；被覆盖内容写入历史快照。
- 断线超过 30 秒显示“连接中断”；恢复连接后先拉取增量，再发送离线队列。
- 大屏只订阅 `published` 数据，不能通过 WebSocket 获取未公开评语或评分。

## 8. `/dashboard` 大屏需求

- 中央：TOP3 动态排名、分数、升降箭头、冠军奖杯/徽章。
- 左侧：维度平均分雷达图、团队轮廓对比、已评/未评进度。
- 右侧：脱敏评语词云、团队互评随机映射关系图（仅管理员模式可见）。
- 底部：最新公示评语摘要跑马灯，单条最多 80 字。
- 数据刷新目标：正常网络下 2 秒内完成页面更新；断线显示最近一次更新时间。
- 默认 16:9 设计，使用 `rem`/`vw` 缩放；21:9 和移动精简模式不得出现横向滚动。
- 大屏需支持全屏、暂停动画、切换 CRT 扫描线和降低动效。

## 9. 响应式要求

- PC（>1024px）：侧边导航、多列评分和完整图表。
- 平板（768–1024px）：双列布局，导航折叠，图表降级。
- 移动端（<768px）：单列瀑布流、底部 Tab、评分全屏 Modal、左右滑查看评语。
- 所有核心输入控件支持键盘操作、触摸操作和错误提示；动效遵循 `prefers-reduced-motion`。

## 10. 非功能指标

- 权限：所有 API 和 WebSocket 连接必须鉴权；评语、成员信息和大屏数据按可见性过滤。
- 性能：评分页面首屏在 4G 网络下目标 ≤3 秒；大屏稳定渲染 60 FPS，低端设备自动关闭 3D 和粒子效果。
- 可靠性：评分提交采用事务写入；消息可重试且不重复计分；所有关键操作可审计。
- 安全：附件类型白名单、大小限制、URL 访问控制；日志不得记录完整隐私数据。
- 可用性：核心评分 API 月度可用性目标 99.5%。

## 11. 验收标准

1. 任意团队可完成资料提交，截止后无法自行修改。
2. Agent 完成前六维评分、教师完成复核后，系统叠加 NO Push 互评并按公式生成 1 位小数总分；非法分值无法发布。
3. 每条评语能按三种可见性正确展示，修改可追溯历史版本。
4. PC 端提交评分后，已授权移动端和大屏在 2 秒内更新；断线恢复后不丢数据、不重复写入。
5. NO Push 映射满足不自评、不重复且全团队覆盖；评分异常时不计入结果并可审计。
6. `/dashboard` 仅展示已发布数据，排名、雷达图、词云、Sankey 和跑马灯数据一致。
7. 375px 移动端、1024px 平板、1920×1080 和 3440×1440 大屏均无核心功能阻塞。
8. 管理员能够锁定比赛、处理异常互评、查看评分版本和操作日志。

## 12. 交付顺序

0. M0.5 Agent 评分 POC 与阈值标定；未通过则启用人工降级路径
1. Design Token 与基础赛博朋克组件
2. PostgreSQL Schema、Redis 缓存及权限模型
3. REST API 与 WebSocket 事件协议
4. 团队/评分/评语/互评页面
5. `/dashboard` 大屏和图表
6. 离线队列、审计、异常处理
7. 语音、3D、粒子和音效等增强功能
