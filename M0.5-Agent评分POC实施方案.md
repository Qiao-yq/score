# M0.5 Agent 评分 POC 实施方案

版本：V1.0（草案）  
日期：2026-08-26  
状态：待产品负责人确认后执行  
依据：[PRD-TASK项目考核打分系统.md](./PRD-TASK项目考核打分系统.md)、[开发流程与实施计划.md](./开发流程与实施计划.md)

> 本方案按当前约束制定：**无真实项目数据（使用模拟样例）+ 离线 mock LLM**。因此 M0.5 本次只验证**工程管线可行性**，不验证 Agent 真实评分质量与阈值标定（详见第 2、12 节）。

---

## 1. 目标

验证「单一 Agent 能稳定产出带可定位证据的前六维评分」这条链路在**工程上**能否跑通，并为 M3 正式开发冻结 Schema、接口和评估框架。

按数据与模型依赖，M0.5 拆成两段：

| 段 | 内容 | 依赖 | 本次是否做 |
|---|---|---|---|
| **M0.5a 可行性 POC** | 验证解析→证据→评分→版本→评估全链路 | 模拟样例 + mock LLM | ✅ 本次做 |
| **M0.5b 阈值标定** | 标定 0.70 / 20 分 / 20% 阈值 | ≥10 真实项目 + ≥2 教师基线 | ❌ 延后 |

> M0.5b 延后后，M3 冻结阈值前必须补齐；期间阈值沿用 PRD 临时常数并显式标注 `rubricVersion = "poc-uncalibrated"`。

---

## 2. 约束与「能验证 / 不能验证」

### 2.1 当前约束

1. 无真实历史项目材料 → 用**模拟样例生成器**构造。
2. 无教师评分基线 → 无法算 MAE、无法做置信度校准。
3. 不接真实 LLM → 用**离线 mock provider**，走同一接口。
4. 无真实视频/在线原型 → 视频用预置转录 mock，原型用本地 HTML 让 Playwright 真跑。

### 2.2 边界结论（重要）

| 能力 | 本次能否验证 |
|---|---|
| 管线连通性（解析→证据编号→评分→版本→评估） | ✅ 能 |
| 证据 Schema / 评分 Schema 的可用性 | ✅ 能 |
| 证据引用校验（禁止引用不存在的证据） | ✅ 能 |
| 重复评分稳定性（评估 harness 本身） | ✅ 能（mock 加噪模式可测） |
| 沙箱安全（禁挂载/限资源/白名单/病毒扫描） | ✅ 能 |
| Agent 真实评分质量、MAE、置信度标定 | ❌ 不能 |
| 正式冻结阈值、宣布 Agent 主评分可用 | ❌ 不能 |

**结论**：本次 POC「通过」仅代表「工程上可行、待真实数据 + 真实 LLM 复验质量」，**不等于**「Agent 评分可用于正式发布」。

---

## 3. 技术选型

| 项 | 选择 | 说明 |
|---|---|---|
| 语言 | Python 3.11+ | 文档/OCR/Playwright/Whisper 生态最顺，POC 迭代快 |
| LLM Provider | `MockLLMProvider`（离线） | 实现与真实 provider 相同的 `score()` 接口；后续可替换为 Claude（Sonnet 5 / Opus 5） |
| 浏览器自动化 | Playwright (Python) | 加载本地 HTML 原型，采集截图/a11y/DOM/console |
| 文档解析 | PyMuPDF + python-docx + markdown | 页码/章节/段落定位 |
| 视频 | 预置 ASR 转录 + 关键帧清单（mock） | 真实 ASR（Whisper）留待有真实视频时接入 |
| 性能采集 | Lighthouse CLI（对本地 HTML 真跑） | 环境/指标名/原始报告定位 |
| 沙箱 | Docker + `--cpus/--memory/--pids-limit/--network` | 禁宿主挂载、白名单出口、ClamAV 扫描 |
| 配置/密钥 | `.env.example` | mock 阶段无需真实密钥 |

---

## 4. 目录结构

```
m0.5-poc/
├── materials/                     # 模拟样例（生成器产物 + 手动预置）
│   ├── generator/                 # 样例生成器
│   │   ├── gen_report.py          # 生成带章节的 Markdown/PDF 报告
│   │   ├── gen_prototype.py       # 生成本地 HTML 原型页面
│   │   ├── gen_meta.py            # 生成提交时间/截止时间等 meta.json
│   │   └── gen_ground_truth.py    # 生成模拟教师评分（供未来 MAE，非本次用）
│   └── samples/
│       └── proj-01/
│           ├── report.md / report.pdf
│           ├── prototype/index.html
│           ├── video.transcript.json   # 预置 ASR（mock）
│           ├── perf.json               # Lighthouse 输出
│           └── meta.json               # 提交时间、截止时间、成员等
├── src/
│   ├── providers/
│   │   ├── base.py               # LLMProvider 抽象接口
│   │   └── mock.py               # 确定性 + 加噪两种 mock 实现
│   ├── parsers/
│   │   ├── document.py           # PDF/Word/MD → 带定位的文本
│   │   ├── browser.py            # Playwright → 截图/a11y/DOM/console
│   │   ├── video.py              # 读取预置转录 + 关键帧（mock）
│   │   ├── perf.py               # 读 Lighthouse JSON / 或触发采集
│   │   └── submit_speed.py       # 提交速度确定性计算（不经 Agent）
│   ├── evidence/
│   │   ├── schema.py             # 证据 JSON Schema
│   │   ├── store.py              # 证据编号 + 存储 + 校验
│   │   └── cite.py               # 评分引用校验（引用必须命中 store）
│   ├── agent/
│   │   ├── score.py              # 评分编排：证据→provider→结构化输出
│   │   ├── prompt.py             # Prompt 模板 + 版本
│   │   └── schema.py             # 评分输出 JSON Schema
│   ├── sandbox/
│   │   ├── Dockerfile
│   │   └── run.py                # docker run 封装（限资源/网络）
│   └── eval/
│       ├── evidence_completeness.py
│       ├── stability.py
│       ├── mae.py                # 有基线时可用，本次标记 skip
│       ├── risk_detection.py
│       ├── latency.py
│       ├── security_check.py
│       └── cost.py               # 记录调用次数/耗时（mock 下为占位）
├── outputs/
│   ├── score_versions/           # 每次评分的不可变 JSON 版本
│   └── reports/                  # 稳定性/误差/成本/决策报告
├── config/
│   └── rubric.json               # 维度/权重/子项/阈值（临时常数 + uncalibrated 标注）
├── .env.example
└── README.md
```

---

## 5. 评分维度与口径（config/rubric.json）

```json
{
  "rubricVersion": "poc-uncalibrated",
  "dimensions": [
    { "key": "submit_speed", "weight": 0.10, "source": "system", "note": "纯自动，Agent/教师不可改" },
    { "key": "report_quality", "weight": 0.30, "source": "agent", "subs": ["完整性","准确性","可执行性"] },
    { "key": "interaction_visual", "weight": 0.20, "source": "agent", "subs": ["原型","布局","色彩","精致度"] },
    { "key": "function_experience", "weight": 0.15, "source": "agent", "subs": ["完整度","流畅度","按钮辨识度"] },
    { "key": "tech_performance", "weight": 0.10, "source": "agent", "subs": ["运行效率","加载速度"] },
    { "key": "presentation", "weight": 0.10, "source": "agent", "subs": ["表现力","答辩质量"] },
    { "key": "peer_review", "weight": 0.05, "source": "nopush", "note": "NO Push 算法，M4 实现，本 POC 不涉及" }
  ],
  "thresholds": {
    "confidence": 0.70,
    "diffReview": 20,
    "modifyRatio": 0.20,
    "calibrated": false
  },
  "submitSpeedRule": { "onTime": 100, "late1h": 80, "late24h": 50, "late72h": 20, "lateOver": 0 }
}
```

**口径决策（待产品负责人确认）**：前六维中「提交速度」由系统确定性计算注入，Agent 实际只对**报告质量 / 交互视觉 / 功能体验 / 技术性能 / 演示效果** 5 个需判断维度出分，最后系统合成前六维结果。此解释与 PRD 5.1「提交速度教师和 Agent 均不可修改」一致。

---

## 6. 证据抽取与编号规范（核心）

### 6.1 证据 JSON Schema

```json
{
  "evidenceId": "DOC-REPORT-001",
  "competitionId": "comp-demo",
  "projectId": "proj-01",
  "dimension": "report_quality",
  "materialType": "pdf",
  "locator": {
    "file": "samples/proj-01/report.pdf",
    "page": 3,
    "section": "2.3 系统架构",
    "paragraph": "采用前后端分离…"
  },
  "extractedAt": "2026-08-26T10:00:00Z",
  "status": "available"
}
```

### 6.2 各材料类型的 locator（对应 PRD 5.4）

| materialType | locator 字段 |
|---|---|
| `pdf` / `docx` / `markdown` | `file` + `page`/`section` + `paragraph` |
| `image` | `file` + `region`(坐标) + `ocrText` |
| `online_prototype` | `url` + `route` + `steps[]` + `screenshot` + `a11yNode` |
| `local_web` | `path` + `steps[]` + `screenshot` + `consoleLog` |
| `video` | `file` + `startTs` + `endTs` + `transcript` + `keyframe` |
| `perf` | `tool` + `env` + `metricName` + `reportLocator` |

### 6.3 证据编号规则

`evidenceId = <TYPE>-<MATERIAL>-<NNN>`，如 `DOC-REPORT-001`、`WEB-PROTOTYPE-002`、`PERF-LIGHTHOUSE-003`。编号全局唯一，写入 store 后不可变。

### 6.4 防编造校验（关键）

评分 Agent 每个维度的 `evidenceIds` **只能引用 store 中已存在的证据**。`src/evidence/cite.py` 在保存评分版本前做硬校验：引用命中 → 通过；引用缺失 → 标记 `riskFlags: ["unresolved_evidence"]` 并触发 `needs_review`。这是「不得凭空生成依据」的程序化保障。

---

## 7. 评分 Agent 设计

### 7.1 输出 Schema（每次评分不可变保存）

```json
{
  "scoreVersion": "v1",
  "rubricVersion": "poc-uncalibrated",
  "inputVersion": "proj-01@2026-08-26T09:00:00Z",
  "dimensions": {
    "report_quality": {
      "score": 85,
      "confidence": 0.82,
      "evidenceIds": ["DOC-REPORT-001", "DOC-REPORT-002"],
      "highlight": "架构分层清晰",
      "suggestion": "补充异常场景"
    }
  },
  "submit_speed": { "score": 100, "source": "system" },
  "riskFlags": [],
  "modelVersion": "mock-provider@1.0",
  "promptVersion": "p1",
  "generatedAt": "2026-08-26T12:00:00Z"
}
```

### 7.2 Prompt 模板要点（prompt.py，版本 p1）

- 角色：单一评分 Agent，按 rubric.json 出分。
- 输入：证据 store（仅注入与该项目相关的证据）+ 材料元数据。
- 约束：每维度必须 `score`(0–100 整数)、`confidence`(0–1)、至少 1 条 `evidenceIds`、`highlight`/`suggestion` 各 ≥10 字。
- 禁止：引用 store 外证据、对「提交速度」出主观分。
- 输出：严格按 7.1 Schema 的 JSON。

### 7.3 Mock Provider 设计（providers/mock.py）

实现 `LLMProvider.score()` 接口，返回合法结构化输出，提供两种模式：

| 模式 | 用途 | 行为 |
|---|---|---|
| `deterministic` | 验证管线连通性 | 规则化出分（按证据数量/质量映射到分数），重复调用结果完全一致 → 稳定性偏差 0 |
| `noisy` | 验证评估 harness 能捕获不稳定 | 在确定性结果上加可控随机扰动，用于测试 stability.py 能正确报偏差 |

> 真实接入时，仅需新增 `providers/claude.py` 实现同一接口，其余管线不变。

---

## 8. 材料模拟策略（materials/generator）

| 材料 | 模拟方式 | 解析器是否真跑 |
|---|---|---|
| 报告 | 生成带章节的真实 Markdown + 转 PDF | ✅ document.py 真解析 |
| 原型 | 生成本地 HTML 页面 | ✅ browser.py 真跑 Playwright |
| 视频 | 预置 `video.transcript.json`（时间戳 + 文本 + 关键帧清单） | ⚠️ video.py 只读 mock 数据 |
| 性能 | 对本地 HTML 跑 Lighthouse，产出 perf.json | ✅ 真采集 |
| 提交速度 | meta.json 写提交时间/截止时间 | ✅ submit_speed.py 确定性计算 |

---

## 9. 沙箱设计（sandbox/）

- `Dockerfile`：基础镜像 + 只读代码挂载，`WORKDIR` 指向一次性工作目录。
- `run.py` 封装 `docker run`：
  - `--cpus=1 --memory=512m --pids-limit=64 --storage-opt size=1G`
  - `--network=none`（默认拒绝）→ 需要时挂白名单网络
  - **不挂载宿主机文件系统**，仅注入待评材料副本
  - 超时 `--stop-timeout` + 外层 watchdog
- 附件进入沙箱前：MIME/扩展名校验 + ClamAV 扫描（`sandbox/security_check.py` 校验）。

---

## 10. 验收映射（7 条 → 可执行测试）

| # | 开发流程验收项 | 测试脚本 | mock 下能否通过 |
|---|---|---|---|
| 1 | 每维 ≥1 条证据、30s 可定位、完整率 ≥90% | `eval/evidence_completeness.py` | ✅ 验证管线与统计逻辑 |
| 2 | 连续 3 次各维偏差 ≤5、结构化字段 100% | `eval/stability.py` | ✅ deterministic=0，noisy 验证 harness |
| 3 | Agent vs 教师 MAE ≤15 | `eval/mae.py` | ❌ 无基线，标记 skip |
| 4 | 缺资料/不可访问标记风险、不编造 | `eval/risk_detection.py` | ✅ 用缺资料样例触发 |
| 5 | 单项目 P95 ≤10min、失败可重试不重复版本 | `eval/latency.py` | ✅ 验证版本幂等逻辑 |
| 6 | 沙箱安全 | `sandbox/security_check.py` | ✅ 真跑 |
| 7 | 成本/吞吐统计 + 压测 | `eval/cost.py` | ⚠️ 占位（mock 无真实成本） |

---

## 11. 分阶段实施步骤

| 步 | 内容 | 产出 | 验收 |
|---|---|---|---|
| M0.5-0 | 骨架 + provider 接口 + mock + 沙箱封装 | 目录 + `run.py` + `mock.py` | 空管线可跑通 |
| M0.5-1 | 样例生成器 + 文档解析 + 证据编号 | `generator/*` + `document.py` + `store.py` | 报告维度出证据 |
| M0.5-2 | 浏览器自动化 + 视频 mock + 性能采集 | `browser.py` + `video.py` + `perf.py` | 5 类材料齐证据 |
| M0.5-3 | 评分 Agent + 结构化输出 + 引用校验 | `score.py` + `cite.py` | 产出评分版本 v1 |
| M0.5-4 | 评估 harness 全量 | `eval/*` | 7 条中 5 条过、1 条占位、1 条 skip |
| M0.5-5 | 汇总报告 + 决策 | `reports/decision.md` | 结论 + 待决清单 |

---

## 12. 决策规则

- **工程通过**：管线连通、Schema 可用、引用校验生效、沙箱安全通过、稳定性 harness 可用 → 冻结 Schema/接口/评估框架，进入 M1，但**阈值保持 `uncalibrated`**。
- **部分通过**：个别解析器或测试未通过 → 保留通过部分，失败项列入降级路径。
- **不通过**：管线核心（证据引用校验 / 沙箱安全）失败 → 回退重设计，不进入 M1。

> 任何情况下，**不因 mock POC 通过而宣布 Agent 主评分可用于正式发布**；该结论须 M0.5b 真实数据复验后由产品负责人作出。

---

## 13. 产出物清单

1. `README.md`：运行说明与 mock 说明
2. `config/rubric.json`：维度/权重/阈值（uncalibrated 标注）
3. 证据 JSON Schema（`evidence/schema.py`）与评分输出 Schema（`agent/schema.py`）
4. 模拟样例生成器 + 一组 `proj-01` 样例
5. 可运行管线（解析→证据→评分→版本）
6. 评估脚本 + 报告（稳定性/证据完整率/风险/耗时/沙箱安全）
7. `reports/decision.md`：结论 + 待决清单 + 降级路径

---

## 14. 待决项（需产品负责人确认）

1. 「提交速度」是否计入「Agent 生成的前六维」，还是仅系统注入（本方案采用后者，见第 5 节）。
2. 开发流程「2–3 项目」与 PRD 5.5「10 项目」口径差异（本方案拆 M0.5a/M0.5b，见第 1 节）。
3. 阈值 0.70/20 分/20% 在标定完成前是否允许以 `uncalibrated` 状态进入 M3 开发。
4. 真实 LLM 选型与预算（标定阶段需接真实 provider）。

> **已决定延后（本次 M0.5 不做）**：「评分标准（Rubric）是否设计为教师可修改」→ 延后到 M1（架构/ER 设计）或 M3（管理员配置评分维度）再设计；本次 M0.5 保持 `rubric.json` 静态冻结，不实现"谁能改规则"的机制。原则先记录：倾向"教师可提议 + 管理员审批 + 版本化生效"，而非"教师自由热改"。

---

## 15. 下一步

确认第 14 节待决项后，即可按第 11 节从 M0.5-0 开始搭建骨架。若需先行，可先产出 `config/rubric.json` 与证据/评分两份 Schema 作为冻结基线。
