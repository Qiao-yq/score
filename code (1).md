# Role
你是一位擅长创意前端开发与全栈架构的资深工程师，精通赛博朋克(Cyberpunk)美学设计、响应式布局及实时数据可视化。请基于以下需求，为我构建一个「TASK项目考核打分系统」。

# Project Overview
本项目不仅是一个考核工具，其本身也应作为技术实力的展示。系统需融合赛博朋克视觉风格，支持PC/移动端实时数据同步，包含专家评语体系，并配备专属结果可视化数据大屏。

# Tech Stack Recommendation
- Frontend: React/Vue3 + TailwindCSS (自定义赛博主题配置) + Framer Motion (故障/霓虹动效)
- Visualization: ECharts/Tremor (大屏图表) + Three.js (可选，用于3D背景装饰)
- Backend: Node.js/NestJS + Socket.io/WebSocket (实现多端实时同步)
- Database: PostgreSQL + Redis (缓存实时评分流)
- Mobile: PWA或响应式H5 (确保移动端体验原生级流畅)

# Visual Design System: Cyberpunk Aesthetic ⚠️核心变更
1. **色彩体系**：
   - 背景：深黑(#0a0a0f) / 深紫蓝(#120c24)
   - 主强调色：霓虹青(#00f3ff) / 激光粉(#ff00aa) / 警告黄(#fcee0a)
   - 文字：高亮白+发光效果(text-shadow/glow)
2. **UI组件特征**：
   - 边框：斜切角(Clip-path)、1px霓虹描边、角落装饰标记
   - 按钮：悬停时触发Glitch(故障)抖动效果或光扫过动画
   - 卡片：半透明毛玻璃(Backdrop-blur) + 内部微光扫描线
   - 字体：标题使用等宽/像素风字体(如Orbitron/Rajdhani)，正文保持可读性无衬线体
3. **氛围营造**：
   - 全局CRT显示器扫描线叠加层(可选开关)
   - 数据加载时使用代码雨/十六进制滚动动画
   - 评分提交成功时触发全屏粒子爆炸或弹窗音效反馈

# Core Business Logic & Data Structure

## 1. 基础信息模型 (Team Entity)
- teamName, captainName, members[{name, role}]
- projectCreativity, businessValue
- attachments: {reportUrl, prototypeUrl, videoUrl}

## 2. 评分体系 (总分100分)
| 维度 | 分值 | 细分指标 | 备注 |
| :--- | :--- | :--- | :--- |
| 提交速度 | 10% | 版本规范、时间戳排序 | 系统纯自动计算，不允许 Agent 或教师修改 |
| 报告质量 | 30% | 完整性(7模块)/准确性/可执行性 | 分项打分 |
| 交互视觉 | 20% | 原型/布局/色彩/精致度 | 赛博风格契合度可作为加分项 |
| 功能体验 | 15% | 完整度/流畅度/按钮辨识度 | 多端一致性检查 |
| 技术性能 | 10% | 运行效率/加载速度 | |
| 演示效果 | 10% | 表现力/答辩质量 | |
| 团队互评 | 5% | NO Push机制贡献度 | 防刷算法 |

## 3. 新增核心功能

### A. 老师评比与评语系统 👨‍🏫
- **结构化评语**：每个打分维度下提供"亮点"与"改进建议"两个文本域
- **快捷标签**：预设赛博风标签如 `#架构优雅` `#交互丝滑` `#文档缺失` `#创意惊艳`，点击即填入
- **语音转文字**(可选)：集成Web Speech API，方便老师口述评语
- **评语可见性控制**：支持设置"仅队长可见"/"全员可见"/"公示到大屏"

### B. 多端实时数据同步 🔄
- **WebSocket通道**：评委在PC端打分/写评语时，移动端/大屏毫秒级更新
- **状态指示器**：页面顶部显示实时连接状态(绿色呼吸灯=已同步，红色闪烁=断连重传)
- **离线暂存**：移动端弱网时本地缓存操作，网络恢复后自动合并上传
- **冲突解决**：以最后写入为准，但保留历史版本快照供审计

### C. 结果可视化数据大屏 📊
独立路由 `/dashboard`，专为投影/大屏幕优化：
1. **中央核心区**：TOP3团队动态排名(带升降箭头动画) + 冠军团队3D奖杯/徽章
2. **左侧面板**：
   - 各维度平均分雷达图(叠加所有团队轮廓对比)
   - 实时打分进度条(已评/未评数量)
3. **右侧面板**：
   - 词云图(提取老师高频评语关键词)
   - 互评贡献度桑基图(Sankey Diagram)展示团队内部协作流向
4. **底部跑马灯**：滚动显示最新提交的评语摘要(脱敏处理)
5. **自适应缩放**：使用rem/vw方案，适配16:9/21:9/超宽屏等各种比例

### D. 成员互评 NO Push 机制
- 固定积分池分配制(不可自评)
- 强制分布校验(极差≥阈值)
- 异常打分模式识别与拦截
- 匿名聚合计算

# Responsive Strategy 📱💻
- **PC端 (>1024px)**：完整Dashboard布局，侧边导航，多列信息展示，复杂图表
- **平板 (768-1024px)**：双列布局，导航收起为汉堡菜单，图表简化
- **移动端 (<768px)**：
  - 单列瀑布流
  - 底部Tab导航
  - 打分面板改为全屏Modal弹出式
  - 大屏模式自动切换为"精简看板"(仅显示排名+核心指标)
  - 触摸手势支持(左滑查看评语，右滑返回)

# Deliverables
请按顺序输出：
1. **赛博朋克Design Token定义** (Tailwind config / CSS Variables)
2. **数据库Schema** (含评语表、实时同步消息队列表)
3. **WebSocket事件协议设计** (Event Name + Payload结构)
4. **大屏组件拆分方案** (含自适应策略)
5. **NO Push互评算法伪代码**
6. **关键赛博风组件代码** (如：霓虹发光评分滑块、故障效果按钮)

请先确认是否理解以上增强需求？特别关注赛博朋克视觉规范与实时同步架构。确认后开始从Design Token输出。
