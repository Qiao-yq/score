# M6 测试与试运行报告

版本：V1.0（对应里程碑 M6）  
状态：单元测试与静态门禁已就绪并通过；集成/压测/安全等**运行时**项待迁移到 Linux/Docker 环境后执行。

---

## 1. 结论摘要

| 项                       | 状态                                                        |
| ------------------------ | ----------------------------------------------------------- |
| 单元测试                 | ✅ 86 用例全通过（6 个 spec）                               |
| 类型检查 / Lint / 构建   | ✅ 4 workspace 全绿                                         |
| 纯函数测试覆盖           | ✅ 评分合成/校验/评语可见性/互评算法/大屏脱敏/WS scope 过滤 |
| 试点数据                 | ✅ `seed-pilot.js`（提交→评分→公示评语→互评映射）           |
| 压测脚本                 | ✅ `infra/load-test/load.mjs`（REST + WS）                  |
| 集成/e2e/压测/安全运行态 | ⏳ 待 Docker/PostgreSQL 环境执行                            |

> 本机（Windows、无 Docker）已覆盖**无副作用纯函数**的全部可测面；需要真实 PostgreSQL / socket.io 服务的集成测试与压测，在 `docker compose -f infra/docker-compose.yml up -d` 后按 §4 执行。

---

## 2. 单元测试清单

运行：`npm run test`（根，等价 `npm run test -w @task/api`，vitest）

| Spec                              | 覆盖点                                                                                                                       | 用例数 |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ------ |
| `scoring/formula.spec.ts`         | 0.7/0.3 合成、四舍五入、提交速度线性插值、最终分加权、权重合计、边界插值/小数互评分                                          | 18     |
| `scoring/validation.spec.ts`      | 维度校验（越界/非整数/置信度/亮点长度/未知维度/子项）、缺失证据、风险标记（低置信/分差/改幅/除零/教师 null）、requiresReview | 20     |
| `scoring/comments.spec.ts`        | 评语长度校验、可见性过滤（captain/all/dashboard × 角色矩阵）                                                                 | 6      |
| `peer-review/no-push.spec.ts`     | 拒绝采样 derangement、双射校验、异常检测、聚合（/100×5 钳制取整）                                                            | 23     |
| `dashboard/text-sanitize.spec.ts` | PII 脱敏（邮箱/手机/学号/负向断言）、码点截断、中文二元组分词与停用词过滤                                                    | 14     |
| `realtime/scope.spec.ts`          | WS 广播 scope 过滤（admin/staff/team/public × 角色矩阵）                                                                     | 5      |

合计 **86**。

### 纯函数模块（无 DB，可长期回归）

- `scoring/formula.ts` / `validation.ts` / `comments.ts`
- `peer-review/no-push.ts`
- `dashboard/text-sanitize.ts`（本次从 `dashboard.service.ts` 抽取）
- `realtime/scope.ts`（本次从 `realtime.service.ts` 抽取）

---

## 3. 集成测试（待运行时环境）

依据《开发流程》§10 集成测试，需真实 PostgreSQL + socket.io。计划项（尚未自动化）：

1. 团队提交 → Agent 评分 → 教师复核 → 管理员批准 → 结果锁定的完整闭环。
2. WebSocket 广播、确认、重试与幂等（`clientMessageId` / `event_outbox` 回放）。
3. 离线队列恢复与冲突处理（服务端最后写入 + 历史快照）。
4. 大屏只读取已发布数据（`status in approved/published`）。
5. Agent 失败 / 低置信度 / 超时 / 资料不可访问的降级路径（needs_review 流转）。

> 建议用 `@nestjs/testing` + 真实 `PrismaClient` 对 SQLite 不可行（schema 用 `@db.Uuid/@db.Timestamptz/@db.Decimal`），故集成/e2e 需以 `infra/docker-compose.yml` 的 Postgres 为准，或后续引入 `testcontainers`。

---

## 4. 运行时验证步骤（Linux/Docker 环境）

```bash
# 1. 启动依赖
docker compose -f infra/docker-compose.yml up -d

# 2. 迁移 + 基础种子 + 试点数据
npm run prisma:migrate -w @task/api
npm run prisma:seed -w @task/api
npm run prisma:seed:pilot -w @task/api

# 3. 启动后端（JWT_SECRET 需覆盖默认值）
npm run dev:api

# 4. 启动前端
npm run dev:web

# 5. 压测（另开终端）
node infra/load-test/load.mjs
```

验收口径：

- 登录 `admin@task.dev` → 大屏展示 2 队排名/雷达/词云/进度/互评映射。
- 观众 `audience@task.dev` 仅在 `dashboardPublished=true`（试点数据已置 true）后可看大屏。
- 压测输出 `rest.errors=0`、`ws.failed=0` 为基线。

---

## 5. 安全检查清单（对照 OWASP）

| 风险             | 实现/待验                                                                                                            |
| ---------------- | -------------------------------------------------------------------------------------------------------------------- |
| A01 越权（IDOR） | 服务端 `AccessService.resolve` + `@Roles`/`@Public` 守卫；每个 service 入口二次校验。**待验**：e2e 越权矩阵          |
| A02 密钥泄露     | `JWT_SECRET` 走 env，`.env` gitignore，仅 `.env.example` 提交；生产走密钥管理。**待办**：CI 校验 JWT_SECRET 非默认值 |
| A03 注入         | Prisma 参数化查询，无拼接 SQL。✅                                                                                    |
| A05 安全配置错误 | CORS `enableCors()` 现为全放开，**上线前收紧 origin 白名单**。⏳                                                     |
| A07 认证/会话    | bcryptjs 哈希；JWT 过期 `JWT_EXPIRES_IN=15m`。✅（提醒：生产替换 `change-me-in-production`）                         |
| A09 日志注入     | 审计 `AuditService.log` 记录 actor/实体/前后值。✅                                                                   |
| XSS              | 前端 React 默认转义；大屏评语走服务端 `sanitizePii`。⚠ 需复核任何 `dangerouslySetInnerHTML`（当前未使用）            |
| 上传安全         | 附件限制 200MB、类型校验 + 病毒扫描（PRD §0.5.6）。**待验**：上传 e2e                                                |
| SSRF/沙箱        | Agent 证据采集需一次性沙箱 + 网络白名单（POC 门禁）。⏳ M0.5 范围                                                    |

---

## 6. 发布门禁对照（《开发流程》§10.4）

| 门禁                     | 现状                             |
| ------------------------ | -------------------------------- |
| P0 用例 100% 通过        | ⏳ 集成/e2e 尚未补齐，暂不能宣布 |
| 严重/高危缺陷为 0        | ⏳ 待运行时扫描                  |
| 性能/长连接/备份演练达标 | ⏳ 待压测（脚本已备）            |
| UAT 签署发布单           | ⏳ 待试运行                      |

---

## 7. 本次改动清单

- 新增 `apps/api/src/dashboard/text-sanitize.ts`（+ `text-sanitize.spec.ts` 14 用例）
- 新增 `apps/api/src/realtime/scope.ts`（+ `scope.spec.ts` 5 用例）
- 补充 `formula.spec.ts`（+4）、`validation.spec.ts`（+5）边界用例
- 新增 `apps/api/prisma/seed-pilot.js` + npm 脚本 `prisma:seed:pilot`
- 新增 `infra/load-test/load.mjs`（REST + WS 压测）
- `dashboard.service.ts` / `realtime.service.ts` 改为复用新纯函数模块
