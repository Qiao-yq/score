# TASK 项目考核打分系统：完整开发流程

版本：V1.0  
依据：[PRD-TASK项目考核打分系统.md](./PRD-TASK项目考核打分系统.md)

## 1. 开发原则

1. 以 PRD 中的评分公式、权限和数据可见性为不可随意变更的业务基线。
2. 先保证评分正确、数据不丢、权限可靠，再实现赛博朋克动效和 3D 装饰。
3. 所有关键业务规则在服务端校验，前端只负责交互和展示。
4. 每个阶段都必须有可运行产物和验收记录，不把问题堆到最后联调。
5. 评分、评语、互评和实时消息均保留可审计版本。

## 2. 推荐团队分工

| 角色 | 责任 |
|---|---|
| 产品/项目负责人 | 需求基线、规则确认、优先级、验收和发布决策 |
| UI/视觉 | Design Token、组件规范、大屏视觉、交互原型 |
| 前端 | 评分端、移动端、大屏、状态管理、WebSocket 客户端 |
| 后端 | 认证授权、评分服务、互评算法、WebSocket、审计 |
| 数据/运维 | Schema、迁移、Redis、备份、监控、部署 |
| QA | 测试用例、自动化测试、兼容性、性能和安全验证 |

小团队可由同一人兼任多个角色，但产品验收与代码提交应尽量分离。

## 3. 里程碑总览（调整后）

| 阶段 | 目标 | 建议产出 |
|---|---|---|
| 阶段 | 建议工期 | 目标 | 主要产出 |
|---|---:|---|---|
| M0 | 1 天 | 需求冻结与风险确认 | PRD 基线、问题清单、验收口径 |
| M0.5 | 2–3 天 | Agent 评分可行性 POC | 证据链样例、稳定性报告、阈值标定、降级决策 |
| M1 | 3 天 | 架构和交互设计 | 架构图、ER 图、API/WebSocket 契约、原型 |
| M2 | 2 天 | 工程基础设施 | 仓库、CI、环境、登录和基础组件 |
| M3 | 6–8 天 | 核心评分闭环 | 团队资料、Agent 评分、教师复核、计算和权限 |
| M4 | 5–7 天 | 互评与实时同步 | NO Push、离线队列、WebSocket、审计 |
| M5 | 3–5 天 | 最小可用大屏与响应式 | `/dashboard`、基础图表、移动端 |
| M6 | 4–5 天 | 测试与试运行 | 测试报告、压测、安全检查、试点数据 |
| M7 | 1–2 天 | 正式发布与运营 | 发布记录、监控、备份、应急预案 |

> 交付策略：M1 与 M2 可并行，M4 与 M5 可并行。小团队先交付核心 MVP（M0–M4，约 15–20 个工作日），大屏高级图表、3D 和复杂动效放入第二迭代（M5–M7，约 8–12 个工作日）。Agent POC 未通过时，M3 自动切换降级路径 B，不得阻塞基础评分上线。

## 4. M0：需求冻结（1–2 天）

### 工作内容

- 评审 PRD 中的角色、Agent/教师合成公式、互评阈值和发布权限。
- 确认比赛时区、截止时间、附件限制和大屏公开范围。
- 建立“待决问题”清单；未确认项不得进入开发分支。
- 将验收标准转换为可执行测试用例编号。

### 退出条件

- 产品负责人签字确认 PRD 版本。
- 所有 P0 规则有唯一解释，尤其是评分和 NO Push 算法。
- 形成变更流程：新增需求必须记录影响范围、优先级和回归项。

## 5. M0.5：Agent 评分 POC（2–3 天）

### 目标

用 2–3 个真实或脱敏项目验证统一 Agent 是否能在真实材料上稳定地产出带证据的前六维评分。POC 是 M3 正式评分开发的前置门禁；未通过时只开发不依赖 Agent 的基础设施，不冻结 Agent Schema。

### 验证范围

- 报告：PDF/Word/Markdown 的章节、页码、段落定位。
- 原型：通过 Playwright 访问 URL，按固定任务路径执行并保存截图、DOM/可访问性节点和控制台日志；无法访问时降级为截图区域定位。
- 视频：ASR 转录、关键帧抽取，使用文件名和起止时间戳定位。
- 性能：执行 Lighthouse/Playwright，保存测试环境、指标名和原始报告定位。
- 提交速度：读取服务端提交时间、截止时间和版本记录，确认纯自动计算。

### 验证用例与通过标准

1. 每个前六维至少生成 1 条可复核证据，证据可由教师在 30 秒内定位；证据完整率 ≥90%。
2. 同一输入连续评分 3 次，各维度最大偏差 ≤5 分，结构化字段通过率 100%。
3. Agent 与基线教师评分的各维 MAE ≤15 分；超过该值的维度进入降级或人工复核。
4. 低质量、缺资料和不可访问链接能正确标记风险，不得编造证据。
5. 单项目 P95 处理时长 ≤10 分钟，失败可重试且不产生重复版本。
6. 运行安全验证：学生项目必须在一次性沙箱中执行，禁止宿主机文件挂载，限制 CPU/内存/磁盘/进程数和执行时长；网络默认拒绝，仅允许白名单出口，禁止访问内网和云元数据地址。附件须先经过文件类型校验和病毒扫描。
7. 成本与吞吐验证：记录单项目 LLM、ASR、浏览器执行和存储成本，统计均值/P95；按目标团队数量和评审窗口压测并发，确认包含失败重试后能完成批量评分。

### POC 产出与决策

- 证据 JSON Schema、评分输出样例、重复评分稳定性报告、误差报告、成本/耗时报告。
- 通过：冻结 Agent 相关 Schema、阈值和 M3 任务，进入 M1。
- 部分通过：仅保留通过的维度，失败维度按降级路径 B 实现。
- 不通过：Agent 仅生成评语和证据，前六维由教师人工评分；不得以未经验证的 Agent 分数发布成绩。

## 5. M1：架构与交互设计（3–5 天）

### 技术设计

- 前端确定 React 或 Vue 3（只选一个），建立 TypeScript 规范。
- 后端确定 NestJS 模块：auth、competition、team、scoring、peer-review、dashboard、audit、realtime。
- PostgreSQL 设计比赛、用户、团队、成员、附件、评分、评语、互评、消息和版本表。
- Redis 规划连接状态、发布订阅、排行榜短缓存和限流。
- 定义 REST API、WebSocket 事件、错误码、幂等键和权限矩阵。
- 确定对象存储方案，不将大文件直接放入数据库。

### 设计产出

- 页面流程图：登录、团队提交、Agent 评分、教师复核、队长团队互评、结果查看、大屏。
- 高保真原型和移动端交互稿。
- Cyberpunk Design Token、组件清单和动效等级（核心/增强/可关闭）。
- 大屏数据聚合口径和脱敏规则。

### 退出条件

- API 和 WebSocket 契约评审通过。
- ER 图可支持版本审计、离线消息和权限过滤。
- 设计稿覆盖 375px、1024px、1920×1080 三种基准尺寸。

## 6. M2：工程基础设施（2–3 天）

### 仓库和代码规范

- 采用 trunk-based 或 `main` + `feature/*` 分支模型；禁止直接向 `main` 推送。
- 配置 ESLint、Prettier、TypeScript strict、提交信息规范和 pre-commit 检查。
- 建立目录：`apps/web`、`apps/api`、`packages/ui`、`packages/contracts`、`infra`。
- API 和 WebSocket 类型放入共享 contracts，避免前后端手写不一致。

### 环境

- `local`：Docker Compose 启动 PostgreSQL、Redis 和对象存储模拟器。
- `staging`：接近生产的独立数据库、域名和 WebSocket 网关。
- `production`：HTTPS、WSS、密钥管理、备份和监控。
- 配置文件只提交 `.env.example`，真实密钥放在密钥管理服务。

### CI 质量门禁

每个 Pull Request 必须通过：类型检查、Lint、单元测试、构建、数据库迁移校验和依赖漏洞扫描。

## 7. M3：核心评分闭环（5–8 天）

按以下顺序开发，保证每一步可独立验收：

1. 登录、角色权限和比赛上下文。
2. 团队/成员 CRUD、资料版本和附件上传。
3. 团队提交、截止时间锁定和管理员解锁。
4. 评分表单、维度校验、报告质量子项和自动提交速度评分。
5. 评语亮点/建议、快捷标签、可见性过滤。
6. Agent 分数与教师复核分合成、管理员批准和最终分计算。
7. 结果查询和基础排名。

### 核心验收

- 同一团队不能重复提交有效版本。
- 客户端篡改权重或分数时服务端拒绝。
- 评分提交使用事务，重复请求不会生成重复最终分。
- 队长、成员、教师、观众看到的数据严格符合权限矩阵。

## 8. M4：互评与实时同步（5–7 天）

### NO Push 实现

- 先实现随机一对一映射（禁止自评、关系不重复且每组被覆盖一次），再实现队长单次评分、双盲访问和异常检测；不实现积分池或成员级聚合。
- 每条互评记录保存原始分配、算法版本、状态、异常原因和处理人。
- 管理员处理异常后重新触发聚合，生成新的结果版本。

### WebSocket 实现

- 先实现评分提交广播，再扩展草稿、评语、进度和大屏事件。
- 服务器为每条消息分配 `serverTime` 和 `entityVersion`。
- 客户端使用 `clientMessageId` 幂等；服务端确认后再删除离线队列。
- 断线重连先拉取增量，随后按顺序发送本地队列。
- 为连接数、消息延迟、重试次数和失败率增加监控指标。

### 验收

- 断网期间移动端可继续录入，恢复后不丢失、不重复计分。
- 两端同时编辑时按服务端最后写入规则处理，并可查看历史快照。
- 未授权客户端无法订阅其他比赛或未发布数据。

## 9. M5：大屏、移动端与视觉实现（5–8 天）

### 实现顺序

1. 先完成无动效的响应式布局和数据占位。
2. 接入排名、雷达图、进度、词云、Sankey 数据接口。
3. 实现大屏自动缩放、全屏和断线提示。
4. 实现移动端底部 Tab、评分全屏 Modal 和触摸手势。
5. 最后加入 Glitch、扫描线、代码雨、粒子和 3D 奖杯。

### 性能约束

- 3D、粒子和背景动画必须可独立关闭。
- 图表更新采用节流和增量更新，避免每条消息重建整个图表。
- 大屏连续运行 8 小时不得出现明显内存增长或页面崩溃。

## 10. 测试流程

### 单元测试

- 评分权重和四舍五入。
- Agent 证据完整性、评分稳定性、置信度和人工兜底分支。
- 提交速度分数。
- 报告质量子项换算。
- NO Push 随机映射、队长单次评分、双盲访问和异常检测。
- 权限判断和数据脱敏。

### 集成测试

- 团队提交到评分完成的完整流程。
- Agent 评分、教师复核改分、管理员批准和结果锁定。
- WebSocket 广播、确认、重试和幂等。
- 离线队列恢复与冲突处理。
- 大屏只读取已发布数据。
- Agent 失败、低置信度、超时和不可访问资料的降级路径。

### 非功能测试

- API 并发和 WebSocket 长连接压测。
- 375px、768px、1024px、1920px、3440px 兼容性测试。
- 键盘操作、对比度、动效关闭和屏幕阅读器基础检查。
- OWASP 常见风险、越权访问、附件上传和 XSS 检查。
- 备份恢复演练和数据库迁移回滚演练。

### 发布门禁

- P0 用例 100% 通过。
- 严重/高危缺陷为 0；中危缺陷有明确豁免记录。
- 性能、长连接和备份演练达到目标。
- 产品负责人完成 UAT 并签署发布单。

## 11. 试运行与上线

### 试运行

- 使用 2–3 个真实或脱敏团队进行小规模试评。
- 模拟截止时间、断网、重复提交、教师未完成复核、异常互评和大屏断线。
- 核对最终分、排名、评语可见性和审计记录。

### 正式上线步骤

1. 冻结代码并生成版本号、变更记录和数据库迁移。
2. 备份生产数据库和对象存储索引。
3. 先部署后端和迁移，再部署前端。
4. 执行冒烟测试：登录、提交、评分、互评、大屏和断线重连。
5. 开启比赛和大屏发布开关。
6. 上线后前 2 小时重点观察错误率、消息延迟、队列积压和数据库连接数。

## 12. 运营与应急

### 日常监控

- API 错误率、P95 延迟、WebSocket 连接数和消息延迟。
- 离线队列积压、评分提交失败率、异常互评数量。
- CPU、内存、数据库连接、Redis 命中率和对象存储容量。

### 应急策略

- WebSocket 故障：自动降级为轮询，评分仍可正常提交。
- Redis 故障：停止实时广播但不影响 PostgreSQL 写入。
- 大屏故障：展示最近一次缓存快照和更新时间。
- 数据异常：暂停发布，保留数据库快照，管理员按审计记录回滚结果版本。
- 所有回滚和人工修正必须记录操作者、原因、时间和影响范围。

## 13. 交付清单

- PRD、原型、设计 Token 和组件规范
- 数据库 ER 图、迁移脚本和种子数据
- API/OpenAPI 文档和 WebSocket 事件协议
- 前端资料端、Agent 评分复核端、移动端和 `/dashboard`
- 后端权限、评分、互评、审计和实时服务
- 自动化测试、性能报告和安全检查报告
- Docker/部署配置、环境变量说明、备份恢复手册
- UAT 签字单、发布记录和已知问题清单
